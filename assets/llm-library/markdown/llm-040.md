## Abstract + Introduction
- 背景
	- Word Embedding是业界的基石: 当时的模型都需要[[Word2Vec]]或者[[Glove]]等预训练词向量, 但是他们是上下文无关的
	- 模型通用性差: NLP各自专注某个任务, 不具备通用性能
	- 迁移学习初步探索: ELMo探索了在无标签数据中探索超越词汇本身的含义, 但是他是基于[[LSTM]]的
- 挑战
	- 如何定义通用? 通过预训练获得通用能力? 如何设置预训练任务呢?
	- 如何实现高效的任务迁移
	- 如何解决长距离依赖问题: [[LSTM]]往往出现长距离信息遗忘和梯度异常的问题
- 贡献
	- 提出一种结合无监督预训练与有监督微调的半监督方法, 使用大量的无监督语料, 以及含有人类标注的监督数据集的两阶段训练过程, 为后序[[LLM]]开创了经典范式
	- 采用了[[Notion/资源(Resourse)/课程与书籍/cs336大模型课程/Transformer|Transformer]]作为核心架构, 也是选定了[[LLM]]的核心框架
## Method
### 无监督预训练
- 构建了一个大规模的, 多样化的文本库
- 任务: 模型需要自监督的, **预测下一个生成的词**, 做到无监督的预训练
### SFT
- 更换预训练的输出层, 换上一个针对特定任务的线性输出层(这个做法有别于现在的做法)
- 然后用下面图所示的任务的标注数据继续训练整个模型
### 数学实现
- 预训练
	- 最大化对数似然, 这个是所有概率模型的基本做法
- 微调目标函数
	- 最大化下游任务的对数损失, 注意, 这里只更新输出层的权重
- 辅助损失函数
	- 将刚才上下的两个损失结合起来, 能够提升模型收敛能力
### 基于Transformer的架构
- 使用了Decoder-only架构, 解决了之前的RNN-类的长距离依赖问题
- 任务相关的输入转换
	- 不修改模型架构, 而是修改输入模型的数据格式, 比如插入一些特殊字符:`<|im_start|>`
![[Pasted image 20251013204942.png|931]]
- 构建了三种任务
	- 文本蕴含: 又称作自然语言推断, 是判断两句话的逻辑关系的一项任务
	- 相似度
	- 多项选择

## Exp
- 在四个大类, 12个细分任务, 9个得到了SOTA, 证明了Transformer架构的优越性
- 在2500个样本的小数据集上, 我们发现性能不如经过多轮训练的BiLSTM模型
## 启示
- 确定了GPT的核心架构与训练方式, 开创了LLM时代
